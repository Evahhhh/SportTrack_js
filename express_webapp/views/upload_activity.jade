html
    head
        title= title
    body
        form(action="/upload", method="post", enctype="multipart/form-data") 
            label Sélectionnez un fichier (JSON uniquement) : 
            br
            input(type='file', name="activites",  id="activites", accept="application/JSON")
            br
            br
            input(type="submit", value="Importer")